// Mayúsculas o Minúsculas
const producto = 'Correo@correo.com';
console.log(producto.toUpperCase());
console.log(producto.toLowerCase());

// Números
const precio = 200;
console.log(precio);
console.log(precio.toString());