// Object Literal
const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 200,
    disponible: true
}

// Manera de saber si un arreglo tiene o no llaves
console.log(Object.keys(objeto));

// Manera de saber si un arreglo tiene o no valores
console.log(Object.values(objeto));

// Nos devuelve todo en pares
console.log(Object.entries(objeto));
