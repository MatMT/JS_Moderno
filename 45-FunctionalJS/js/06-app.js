// Funciones Puras
const num1 = 20;
const duplicar = n => n * 2;

// Retorna la misma cantidad de entrada | No modifica un valor original
const resultado = duplicar(num1);

console.log(num1);
console.log(resultado);